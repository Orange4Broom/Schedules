import { DefaultLayout } from '@layouts/defaultLayout/DefaultLayout';
import { FC } from 'react';

export const App: FC = () => {
  return (
    <DefaultLayout>
      <h1>Hello, world!</h1>
    </DefaultLayout>
  );
};
